{
  "name": "game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "build": "esbuild index.ts --bundle --outfile=jogo.js --platform=browser",
  "watch": "esbuild index.ts --bundle --outfile=jogo.js --platform=browser --watch"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}